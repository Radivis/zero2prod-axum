# Domain for Caddy TLS (e.g., yourdomain.com)
DOMAIN=localhost

# App
APP_BASE_URL=https://yourdomain.com
APP_HMAC_SECRET=change-me-to-a-long-random-string
APP_EMAIL_CLIENT__SENDER_EMAIL=your-postmark-approved-sender@example.com

# Postgres
POSTGRES_APP_USER=app
POSTGRES_APP_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=newsletter

# Email (Postmark)
POSTMARK_API_TOKEN=your-postmark-api-token

# Grafana (Logging UI)
GRAFANA_ADMIN_PASSWORD=replace-with-a-strong-grafana-admin-password
